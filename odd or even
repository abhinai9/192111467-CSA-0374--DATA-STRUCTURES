#include <stdio.h>
int main()
{
    int num1,rem1;
    printf("enter an number: ");
    scanf("%d", &num1);
    rem1 = num1 % 2;
    if (rem1 == 0)
        printf("%d is an even number\n", num1);
    else
        printf("%d is an odd number\n", num1);
}
